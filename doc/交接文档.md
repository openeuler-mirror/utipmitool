```bash
git branch  -a 
a
back_dev_tran
backup
backup1
backup_refactor
develop/trans #之前的主线开发分支，可以不管了。
lan
master #打包后的压缩，一般在主线开发分支或者trans分支打包后复制到这个分支。
refactor
review-chassis-user
review-lan-cmd
review-mc-cmd
review-mc-tmp
review-sdr-format
review-user-cmd
review-user-tmp
snipe-review #主线开发分支，提交到桌面的 snipe develop/trans
trans #服务器代码分支，定期从主线pickup，对应origin/trans。
remotes/origin/HEAD -> origin/master
remotes/origin/master
remotes/origin/trans
remotes/snipe/develop/trans
remotes/snipe/master
```



打rpm压缩包命令参考build.sh1

```shell
git checkout snipe-review
bash build.sh1 #只压缩打包
bash build.sh1 true #打rpm

#然后切换到master分支,讲打包的压缩文件覆盖master的压缩文件
cp server/utipmitool-0.9.0.tar.gz  .
后面就是修复utipmitool.spec并提交了。
```

debian 打包

```shell
 git checkout snipe-review
 dpkg-buildpackage -tc -uc -ts
```





 